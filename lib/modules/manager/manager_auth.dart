import 'package:flutter/material.dart';

class ManagerAuth extends StatelessWidget {
  const ManagerAuth({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
